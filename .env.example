# Optional API key for POST /predict (leave empty to disable auth locally)
API_KEY=

# Optional Streamlit auth (ui.auth_enabled must be true in configs/config.yaml)
STREAMLIT_AUTH_PASSWORD=

# Override log level (optional)
LOG_LEVEL=INFO
